# 开发贡献指南

本指南适用于维护 ones-agent 仓库的人和编码 Agent。新增及修改代码遵循以下规范；历史代码按改动范围逐步整理，避免无关的全库格式化。

## 规范入口

| 文档 | 内容 |
| --- | --- |
| [代码规范](docs/code_standards.md) | Python 风格、类型、配置、异常、安全与测试 |
| [架构规范](docs/architecture_standards.md) | 模块职责、依赖方向、契约、状态和扩展边界 |
| [Git 工作流规范](docs/git_workflow.md) | 分支、提交、审查、同步、合并与回退 |

开始前阅读根目录及目标目录的 `AGENTS.md`（如有）。修改产品内的审批、发布、状态流转或 TUI 时，必须同时阅读 [工作流长期约束](docs/developer_workflow_constraints.md)。专项约束不可被本指南的一般规则放宽；发现文档与实现不一致时，在变更中明确指出并修复相关文档，不能据此绕过门禁。

## 本地开发

使用 Python 3.11+ 和 Git，在仓库根目录运行：

```console
uv sync --group dev
uv run ones-dev --help
uv run pytest
```

终端界面入口为 `uv run ones-dev tui`；后端和 MCP 入口见 [README](README.md)。当前项目没有网页前端，无需前端构建。

## 交付要求

1. 明确问题、影响范围和可观察的验收结果；跨模块、持久化或行为契约变更先说明设计与兼容策略。通过 OpenSpec 生成的规范使用中文。
2. 按职责修改实现，同步相关契约、文档和必要的回归测试。
3. 执行与改动匹配的检查，记录命令、结果及未验证项。仅文档变更检查链接、示例和差异即可。
4. 按 Git 工作流提交和审查；PR 描述包含行为变化、验证证据、兼容性及回退方式。

文档约定不代表托管平台已启用强制检查。新增 CI、格式化或类型检查工具时，应同时提交配置、依赖和执行说明。
